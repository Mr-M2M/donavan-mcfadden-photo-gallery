<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:label="@string/app_name"
        android:theme="@style/Theme.PhotoGallery"
        android:supportsRtl="true">

        <!-- Splash screen: shows student name + "My Photo Gallery" -->
        <activity
            android:name=".MainActivity"
            android:theme="@style/Theme.PhotoGallery.Splash"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Main activity: 6 thumbnails -->
        <activity
            android:name=".GalleryActivity"
            android:exported="false" />

        <!-- Secondary activity: enlarged image view -->
        <activity
            android:name=".ImageDetailActivity"
            android:exported="false"
            android:parentActivityName=".GalleryActivity" />

    </application>

</manifest>
